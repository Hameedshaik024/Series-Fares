// Edit this before deploying: point it at your deployed backend (Render/Railway URL).
// For local testing, leave it as the local Flask dev server.
window.CONFIG = {
  API_BASE_URL: "http://localhost:5000",
};
